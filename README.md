Create A a free API key from Google Gemini API and use the API key 

Note: the api key is not stored anywhere nor is sent to any server Rather it as as a temporary key and once you refresh the page the key vanish.
